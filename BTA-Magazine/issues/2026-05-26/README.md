# Issue 2026-05-26

This folder is the example issue.

To replace it with a real issue, edit:

- `viewer.json` for page images, cover image, title, table of contents, and alt text.
- `publish_manifest.json` for branding, background, music, and feature settings.

Keep the folder name in `YYYY-MM-DD` format.
